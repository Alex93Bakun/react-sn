import React from 'react';

const AddMessageForm = props => {
    return (
        <form>
            <div>
                <textarea/>
            </div>
            <div>
                <button>Send</button>
            </div>
        </form>
    );
};

export default AddMessageForm;
